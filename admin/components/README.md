# Admin Components

Suggested folders:

- `layout`: shell, sidebar, topbar.
- `dashboard`: KPI cards and charts.
- `drivers`: driver verification UI.
- `orders`: order table and order detail UI.
- `pricing`: fare forms and surcharge rules.
- `payouts`: payout tables and approval dialogs.
- `ui`: reusable buttons, inputs, badges, modals.

